services:
    bot:
        build:
            context: .
            dockerfile: ./Dockerfile.node
        environment:
            NODE_ENV: production
        restart: always
        depends_on:
            - redis

    redis:
        image: redis
        restart: always
